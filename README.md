# MCM2017: Tasks Pricing

- `dataset.py` loading data
- `view.py` plotting on a map
- `data/` three datasets
- `map_view/` plotted maps

**Note**: maps under `map_view/` is too big for browsers to render.